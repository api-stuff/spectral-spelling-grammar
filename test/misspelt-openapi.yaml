openapi: 3.0.3
info:
  title: Misspelt Words API proof-of-concpte
  description: A collection of bdaly smealt words
  version: 1.0.0
paths:
  /test:
    get:
      summary: Defautl endpoint
      description: My speling is veeeryy poor
      responses:
        "200":
          description: Defalt response
          content:
            application/json:
              schema:
                type: string
        "400":
          description: Correctly spelt phrase that will not be reported
          content:
            application/json:
              schema:
                type: string
